export const ENV = {
  PROD: 'PROD'
}

export const MGBOX_ENDPOINT: { [key: string]: string } = {
  PROD: 'https://box.magic.link'
}

/* Managed by jerry@fortmatic.com */
export const API_KEY: { [key: string]: string } = {
  PROD: 'YOUR_PUBLISHABLE_API_KEY'
}
